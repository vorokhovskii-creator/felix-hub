# 🌍 Многоязычная система Felix Hub

## Обзор

Felix Hub теперь поддерживает три языка:
- 🇬🇧 **Английский** (English)
- 🇮🇱 **Иврит** (עברית) с поддержкой RTL
- 🇷🇺 **Русский**

## Установка и настройка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Выполните миграцию базы данных

```bash
python migrations/add_multilang_support.py
```

Эта миграция добавит новые поля в таблицу `parts`:
- `name_en`, `name_he`, `name_ru` - названия на трех языках
- `description_en`, `description_he`, `description_ru` - описания

### 3. Перезапустите приложение

```bash
python app.py
```

## Использование

### Для администратора

#### Добавление запчасти с переводами

1. Зайдите в админ-панель: `/admin/parts`
2. Нажмите кнопку **"+ Запчасть"**
3. Заполните форму:

```
🌍 Названия на трех языках
- Название на русском* (обязательно)
- Название на английском
- Название на иврите

📝 Описания (опционально)
- Описание на русском
- Описание на английском
- Описание на иврите

⚙️ Настройки
- Категория*
- Порядок сортировки
- Активна (чекбокс)
```

4. Нажмите **"Сохранить"**

#### Пример заполнения

**Русский:**
- Название: `Передние колодки`
- Описание: `Керамические тормозные колодки для передних колес`

**Английский:**
- Name: `Front brake pads`
- Description: `Ceramic brake pads for front wheels`

**Иврит:**
- שם: `רפידות בלם קדמיות`
- תיאור: `רפידות בלם קרמיות לגלגלים הקדמיים`

### Для механика

#### Переключение языка интерфейса

1. В правом верхнем углу найдите переключатель языка: 🌍
2. Выберите нужный язык из списка
3. Интерфейс и названия запчастей автоматически переключатся

**При выборе иврита:**
- Направление текста меняется на RTL (справа налево)
- Все элементы интерфейса зеркально отображаются
- Запчасти показываются с ивритскими названиями

#### Сохранение предпочтений

- Выбранный язык сохраняется в `localStorage`
- При следующем входе язык восстанавливается автоматически
- Для авторизованных механиков язык также сохраняется в профиле

## API

### Получение запчастей на определенном языке

```javascript
// Получить запчасти на английском
fetch('/api/parts?lang=en')

// Получить запчасти на иврите
fetch('/api/parts?lang=he')

// Получить запчасти на русском (по умолчанию)
fetch('/api/parts?lang=ru')
```

### Получение каталога

```javascript
// Каталог вернет названия на выбранном языке
fetch('/api/parts/catalog?lang=en')
```

### Создание/обновление запчасти

```javascript
fetch('/api/admin/parts', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        name_ru: 'Передние колодки',
        name_en: 'Front brake pads',
        name_he: 'רפידות בלם קדמיות',
        description_ru: 'Описание',
        description_en: 'Description',
        description_he: 'תיאור',
        category: 'Тормоза',
        is_active: true
    })
})
```

## Структура базы данных

### Таблица `parts`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Уникальный ID |
| `name_ru` | VARCHAR(250) | Название на русском* |
| `name_en` | VARCHAR(250) | Название на английском |
| `name_he` | VARCHAR(250) | Название на иврите |
| `description_ru` | TEXT | Описание на русском |
| `description_en` | TEXT | Описание на английском |
| `description_he` | TEXT | Описание на иврите |
| `category` | VARCHAR(120) | Категория |
| `is_active` | BOOLEAN | Активна |
| `sort_order` | INTEGER | Порядок сортировки |

*Обязательное поле

### Таблица `mechanics`

Добавлено поле `language` (VARCHAR(5)) для хранения предпочтительного языка механика.

## Модели

### Part.to_dict(lang=None)

```python
# Для механика - возвращает только выбранный язык
part.to_dict(lang='en')
# => { 'id': 1, 'name': 'Front brake pads', 'description': '...', ... }

# Для админа - возвращает все языки
part.to_dict()
# => { 'id': 1, 'name_ru': '...', 'name_en': '...', 'name_he': '...', ... }
```

### Part.get_name(lang='ru')

```python
part.get_name('en')  # => 'Front brake pads'
part.get_name('he')  # => 'רפידות בלם קדמיות'
part.get_name('ru')  # => 'Передние колодки'
```

## Frontend

### Переключатель языка

Автоматически добавляется на все страницы при подключении:

```html
<link rel="stylesheet" href="/static/css/language-switcher.css">
<script src="/static/js/language-switcher.js"></script>
```

### Использование в JavaScript

```javascript
// Получить текущий язык
const lang = getCurrentLanguage();

// Загрузить данные с учетом языка
fetch(`/api/parts?lang=${lang}`)
```

## RTL (Right-to-Left) поддержка

При выборе иврита автоматически:
- `document.dir = 'rtl'`
- Все элементы зеркально отображаются
- Таблицы, формы, кнопки выравниваются справа

## Обратная совместимость

Старое поле `name` сохранено для совместимости. При миграции:
```python
# Автоматически копируется в name_ru
UPDATE parts SET name_ru = name WHERE name_ru IS NULL
```

## Рекомендации

1. **Всегда заполняйте русский язык** - он используется как fallback
2. **Переводите системные запчасти** - для лучшего UX
3. **Проверяйте RTL** - тестируйте интерфейс на иврите
4. **Используйте короткие названия** - для лучшей читаемости в списках

## Troubleshooting

### Запчасти не переключаются на другой язык

1. Проверьте, что миграция выполнена
2. Убедитесь, что заполнены поля для всех языков
3. Проверьте console в браузере на ошибки

### RTL работает неправильно

1. Очистите кеш браузера
2. Проверьте, что CSS файл загружен: `/static/css/language-switcher.css`
3. Убедитесь, что `document.dir` установлен в 'rtl'

### Язык не сохраняется

1. Проверьте localStorage в DevTools
2. Для авторизованных - проверьте поле `language` в таблице `mechanics`

## Примеры использования

### Пример 1: Список запчастей на иврите

```javascript
fetch('/api/parts?lang=he&category=Тормоза')
    .then(r => r.json())
    .then(parts => {
        parts.forEach(part => {
            console.log(part.name); // => "רפידות בלם קדמיות"
        });
    });
```

### Пример 2: Форма заказа с выбранным языком

```javascript
const lang = getCurrentLanguage();
const catalog = await fetch(`/api/parts/catalog?lang=${lang}`).then(r => r.json());

// Отобразить категории и запчасти на языке пользователя
Object.entries(catalog).forEach(([category, parts]) => {
    console.log(`Категория: ${category}`);
    parts.forEach(part => console.log(`  - ${part}`));
});
```

## Следующие шаги

- [ ] Добавить переводы для категорий
- [ ] Локализовать статусы заказов
- [ ] Добавить языковые файлы для всех шаблонов
- [ ] Интеграция с Telegram на разных языках

---

**Версия:** 2.2  
**Дата:** 3 ноября 2025  
**Автор:** Felix Hub Team
