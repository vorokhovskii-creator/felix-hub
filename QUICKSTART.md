# Felix Hub 2.1 - Быстрый старт

## 🚀 Запуск приложения (всего 3 шага!)

### Шаг 1: Установка зависимостей

```bash
# Выполните этот скрипт один раз при первой установке
./start.sh
```

### Шаг 2: Настройка (опционально)

Если хотите использовать Telegram-уведомления, отредактируйте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
```

> **Примечание**: Приложение работает и без Telegram!

### Шаг 3: Запуск

```bash
# Каждый раз когда хотите запустить приложение
./start.sh
```

Или вручную:

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Запустить приложение
python app.py
```

## 📱 Доступ к приложению

После запуска откройте в браузере:

- **Главная страница**: http://localhost:5000
- **Для механиков**: http://localhost:5000/mechanic?lang=ru
- **Панель администратора**: http://localhost:5000/admin
  - Пароль: `felix2025`

## 📲 Мобильный доступ

Чтобы механики могли использовать приложение с телефонов в локальной сети:

1. Узнайте IP-адрес вашего компьютера:
   ```bash
   # macOS/Linux
   ifconfig | grep "inet " | grep -v 127.0.0.1
   
   # Или
   hostname -I
   ```

2. Механики могут открыть в браузере телефона:
   ```
   http://ВАШ_IP:5000
   ```
   Например: `http://192.168.1.100:5000`

## 🔧 Основные функции

### ✅ Что уже работает из коробки:

- 🌐 Трёхъязычный интерфейс (RU/EN/HE)
- 📱 Мобильная версия для механиков
- 👨‍💼 Панель администратора
- 🗄️ База данных SQLite
- 🚗 Работа с гос номерами
- 📋 Каталог запчастей
- 📊 Статистика заказов
- 🖨️ Печать чеков
- 🔄 Автообновление данных

### 🔔 Что требует настройки:

- Telegram-уведомления (нужен бот)
- HTTPS (для продакшена)
- PostgreSQL (вместо SQLite для больших объёмов)

## 🎯 Типичный сценарий использования

1. **Механик Давид** открывает приложение на телефоне
2. Выбирает **Русский** язык
3. Заполняет форму:
   - Имя: Давид
   - Гос номер: **123-45-678**
   - Категория: Тормоза
   - Детали: Передние колодки, Диски передние
   - Тип: Аналог
4. Отправляет заказ
5. **Администратор** видит новый заказ в панели
6. Находит запчасти и меняет статус на **"Готово"**
7. **Давид** получает уведомление в Telegram (если настроено)
8. Чек автоматически печатается

## 📝 Изменение каталога запчастей

Откройте `app.py` и найдите:

```python
PARTS_CATALOG = {
    'Тормоза': ['Передние колодки', 'Задние колодки', ...],
    'Двигатель': ['Масло моторное', ...],
    # Добавьте свои категории
    'Ваша категория': ['Деталь 1', 'Деталь 2', ...],
}
```

## 🔒 Изменение пароля администратора

Откройте `app.py` и найдите функцию `admin_login()`:

```python
if request.form.get('password') == 'felix2025':  # Измените здесь
```

Для безопасности рекомендуется использовать хеширование паролей (Flask-Login + werkzeug.security).

## 🐛 Решение проблем

### Порт 5000 занят

```bash
# Найти процесс на порту 5000
lsof -i :5000

# Убить процесс
kill -9 PID
```

Или измените порт в `app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=8000)  # Вместо 5000
```

### База данных не создаётся

```bash
# Удалите старую БД и пересоздайте
rm felix_hub.db
python app.py
```

### Ошибка импорта модулей

```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

## 📊 Структура проекта

```
felix-hub-2.1/
├── app.py                  # Главный файл приложения
├── requirements.txt        # Зависимости Python
├── .env                   # Конфигурация (не коммитить!)
├── .env.example           # Пример конфигурации
├── start.sh              # Скрипт быстрого запуска
├── README.md             # Документация разработчика
├── USER_GUIDE.md         # Руководство пользователя
├── QUICKSTART.md         # Этот файл
├── templates/            # HTML шаблоны
│   ├── index.html        # Главная страница
│   ├── mechanic.html     # Интерфейс механика
│   ├── admin.html        # Панель администратора
│   └── admin_login.html  # Вход администратора
├── static/               # Статические файлы
│   └── uploads/          # Загруженные файлы
└── felix_hub.db          # База данных SQLite (создаётся автоматически)
```

## 🚀 Деплой на сервер (продакшен)

### Вариант 1: Простой (Heroku)

```bash
# Создайте файл Procfile
echo "web: gunicorn app:app" > Procfile

# Добавьте gunicorn в requirements.txt
echo "gunicorn==21.2.0" >> requirements.txt

# Деплой
heroku create felix-hub
git push heroku main
```

### Вариант 2: VPS (Ubuntu)

```bash
# Установите зависимости
sudo apt update
sudo apt install python3-pip python3-venv nginx

# Клонируйте проект
git clone [ваш-репозиторий]
cd felix-hub-2.1

# Установите зависимости
./start.sh

# Настройте Gunicorn
pip install gunicorn

# Запустите
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

### Вариант 3: Docker

```dockerfile
# Создайте Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
```

```bash
# Соберите и запустите
docker build -t felix-hub .
docker run -p 5000:5000 felix-hub
```

## 💡 Полезные команды

```bash
# Бэкап базы данных
cp felix_hub.db felix_hub_backup_$(date +%Y%m%d).db

# Просмотр логов (если запущено в фоне)
tail -f nohup.out

# Остановка приложения
# Нажмите Ctrl+C в терминале

# Проверка версии Python
python3 --version

# Список установленных пакетов
pip list

# Обновление всех пакетов
pip install --upgrade -r requirements.txt
```

## 🎉 Готово!

Ваше приложение Felix Hub 2.1 готово к использованию!

**Следующие шаги:**

1. ✅ Запустите приложение: `./start.sh`
2. ✅ Откройте http://localhost:5000
3. ✅ Протестируйте создание заказа
4. ✅ Зайдите в админ-панель (пароль: felix2025)
5. 🔔 Настройте Telegram (опционально)
6. 🚀 Поделитесь с командой!

---

**Нужна помощь?** Проверьте `USER_GUIDE.md` или `README.md`
