# 🔧 Исправление базы данных на Render

## Проблема

При импорте каталога запчастей на Render возникает ошибка:
```
column categories.name_en does not exist
```

**Причина:** В базе данных PostgreSQL на Render не выполнены миграции для добавления колонок многоязычности.

## ✅ Решение

### Вариант 1: Через веб-консоль Render (Рекомендуется)

1. **Откройте Render Dashboard**
   - Перейдите на https://dashboard.render.com/
   - Выберите ваш сервис `felix-hub`

2. **Откройте Shell**
   - Нажмите на кнопку "Shell" в правом верхнем углу
   - Подождите, пока откроется терминал

3. **Запустите миграцию**
   ```bash
   python init_render_db.py
   ```

4. **Проверьте результат**
   - Вы должны увидеть сообщения о добавлении колонок
   - Должно быть "✅ БАЗА ДАННЫХ ПОЛНОСТЬЮ ГОТОВА К РАБОТЕ"

### Вариант 2: Через Render CLI

Если у вас установлен Render CLI:

```bash
# Подключитесь к сервису
render shell felix-hub

# Запустите миграцию
python init_render_db.py
```

### Вариант 3: Пересоздание базы данных (если предыдущие не работают)

**⚠️ ВНИМАНИЕ:** Это удалит все данные!

1. **Создайте резервную копию** (если есть важные данные)

2. **В Render Dashboard:**
   - Перейдите в раздел Databases
   - Найдите вашу PostgreSQL БД
   - Нажмите "Settings" → "Danger Zone"
   - Нажмите "Suspend Database"
   - Создайте новую БД или возобновите текущую

3. **После пересоздания:**
   - Зайдите в Shell вашего сервиса
   - Запустите: `python init_render_db.py`

## 🧪 Проверка

После выполнения миграции проверьте:

1. **Откройте админ-панель:**
   ```
   https://felix-hub.onrender.com/admin/login
   ```

2. **Попробуйте импортировать каталог запчастей**
   - Перейдите в раздел "Управление запчастями"
   - Нажмите "Импорт из Excel"
   - Выберите файл каталога
   - Ошибка должна исчезнуть

3. **Проверьте категории**
   - В админ-панели должны отображаться категории на всех языках

## 📋 Что делает init_render_db.py

Скрипт автоматически:
1. ✅ Создаёт все таблицы (если их нет)
2. ✅ Проверяет наличие колонок многоязычности
3. ✅ Добавляет недостающие колонки:
   - `categories.name_en`, `name_he`, `name_ru`
   - `parts.name_en`, `name_he`, `description_en`, `description_he`, `description_ru`
4. ✅ Обновляет переводы для стандартных категорий
5. ✅ Выводит статистику по базе данных

## 🔍 Проверка структуры БД

Чтобы убедиться, что колонки добавлены, выполните в Shell:

```bash
python -c "
from app import app, db
from sqlalchemy import inspect

with app.app_context():
    inspector = inspect(db.engine)
    columns = inspector.get_columns('categories')
    print('Колонки таблицы categories:')
    for col in columns:
        print(f'  - {col[\"name\"]}: {col[\"type\"]}')"
```

Вы должны увидеть:
```
Колонки таблицы categories:
  - id: INTEGER
  - name: VARCHAR(120)
  - name_en: VARCHAR(120)
  - name_he: VARCHAR(120)
  - name_ru: VARCHAR(120)
  - is_active: BOOLEAN
  - sort_order: INTEGER
  - created_at: TIMESTAMP
  - updated_at: TIMESTAMP
```

## 📝 Альтернативный метод (SQL напрямую)

Если ничего не помогает, можно добавить колонки напрямую через SQL:

1. **Подключитесь к PostgreSQL** (через Render Dashboard → Database → External Connection)

2. **Выполните SQL:**
   ```sql
   -- Для категорий
   ALTER TABLE categories ADD COLUMN IF NOT EXISTS name_en VARCHAR(120);
   ALTER TABLE categories ADD COLUMN IF NOT EXISTS name_he VARCHAR(120);
   ALTER TABLE categories ADD COLUMN IF NOT EXISTS name_ru VARCHAR(120);

   -- Для запчастей
   ALTER TABLE parts ADD COLUMN IF NOT EXISTS name_en VARCHAR(250);
   ALTER TABLE parts ADD COLUMN IF NOT EXISTS name_he VARCHAR(250);
   ALTER TABLE parts ADD COLUMN IF NOT EXISTS description_en TEXT;
   ALTER TABLE parts ADD COLUMN IF NOT EXISTS description_he TEXT;
   ALTER TABLE parts ADD COLUMN IF NOT EXISTS description_ru TEXT;

   -- Обновление переводов категорий
   UPDATE categories SET name_ru = 'Тормоза', name_en = 'Brakes', name_he = 'בלמים' WHERE name = 'Тормоза';
   UPDATE categories SET name_ru = 'Двигатель', name_en = 'Engine', name_he = 'מנוע' WHERE name = 'Двигатель';
   UPDATE categories SET name_ru = 'Подвеска', name_en = 'Suspension', name_he = 'מתלים' WHERE name = 'Подвеска';
   UPDATE categories SET name_ru = 'Электрика', name_en = 'Electrical', name_he = 'חשמל' WHERE name = 'Электрика';
   UPDATE categories SET name_ru = 'Расходники', name_en = 'Consumables', name_he = 'מתכלים' WHERE name = 'Расходники';
   UPDATE categories SET name_ru = 'Добавки', name_en = 'Additives', name_he = 'תוספים' WHERE name = 'Добавки';
   UPDATE categories SET name_ru = 'Типуль', name_en = 'Maintenance', name_he = 'טיפול' WHERE name = 'Типуль';
   ```

## 🚀 После исправления

После успешного выполнения миграции:
1. ✅ Импорт каталога запчастей заработает
2. ✅ Многоязычность будет работать корректно
3. ✅ Все API будут возвращать данные на трёх языках

## 📞 Если проблема остаётся

Если после всех действий ошибка сохраняется:
1. Проверьте логи Render: Dashboard → Logs
2. Убедитесь, что используется правильная база данных (проверьте DATABASE_URL в Environment)
3. Попробуйте перезапустить сервис: Dashboard → Manual Deploy → Clear build cache & deploy
