# 🔧 Исправление базы данных на Render

## Проблема
База данных PostgreSQL на Render не инициализирована. Таблицы не созданы.

**Ошибка:**
```
psycopg2.errors.UndefinedTable) relation "mechanics" does not exist
```

## Решение

### Вариант 1: Через Shell (быстро) ✅

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Перейдите в ваш сервис `felix-hub`
3. Нажмите на кнопку **Shell** в верхнем меню
4. Выполните команду:
   ```bash
   python init_render_db.py
   ```
5. Дождитесь сообщения `✅ Таблицы созданы`
6. Обновите страницу в браузере

### Вариант 2: Через API Render

Если Shell недоступен, используйте Render API для выполнения команд.

### Вариант 3: Пересоздать сервис

1. Удалите текущий web service
2. Создайте новый с теми же настройками
3. При создании убедитесь, что `buildCommand` содержит:
   ```bash
   pip install -r requirements.txt && python init_render_db.py
   ```

## Проверка

После инициализации проверьте:

1. Откройте https://felix-hub.onrender.com/admin/login
2. Войдите с паролем `felix2025`
3. Перейдите в "Механики"
4. Попробуйте добавить механика

Список должен загрузиться без ошибок.

## Что было сделано локально

- ✅ Создан скрипт `init_render_db.py`
- ✅ Добавлен в `buildCommand` в `render.yaml`
- ✅ Исправлены декораторы API для возврата JSON вместо HTML (коммит)

## Текущее состояние кода

Код в репозитории **исправлен и готов к работе**. Проблема только в том, что база данных на Render не была проинициализирована при первом деплое.

## Дополнительно

Если проблема повторится, можно добавить автоматическую инициализацию в сам `app.py`:

```python
# В функции init_db() в app.py
def init_db():
    with app.app_context():
        try:
            db.create_all()
            print("✅ База данных инициализирована")
        except Exception as e:
            print(f"❌ Ошибка инициализации БД: {e}")
```

Это уже есть в коде и вызывается при старте приложения.
