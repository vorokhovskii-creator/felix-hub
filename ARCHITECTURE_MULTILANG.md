# 📊 Схема работы многоязычной системы

## Архитектура решения

```
┌─────────────────────────────────────────────────────────────────┐
│                        БАЗА ДАННЫХ                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────┐    ┌─────────────────────────────┐   │
│  │   Categories         │    │   Parts                      │   │
│  ├──────────────────────┤    ├─────────────────────────────┤   │
│  │ id: 1                │    │ id: 15                       │   │
│  │ name: "Двигатель"    │    │ name_ru: "Масляный фильтр"  │   │
│  │ name_ru: "Двигатель" │    │ name_en: "Oil filter"       │   │
│  │ name_en: "Engine"    │    │ name_he: "פילטר שמן"        │   │
│  │ name_he: "מנוע"      │    │ category: "Двигатель"       │   │
│  └──────────────────────┘    └─────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │   Orders                                                    │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ id: 123                                                     │ │
│  │ mechanic_name: "Иван"                                       │ │
│  │ category: "Двигатель"                                       │ │
│  │ selected_parts: [                                           │ │
│  │   {                                                         │ │
│  │     "part_id": 15,         ← КЛЮЧ ДЛЯ ПЕРЕВОДА            │ │
│  │     "name": "פילטר שמן",   ← Название на языке механика   │ │
│  │     "quantity": 2                                           │ │
│  │   }                                                         │ │
│  │ ]                                                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Поток данных

### 1️⃣ Механик создает заказ (иврит)

```
┌──────────────────┐
│  Браузер         │
│  (lang=he)       │
└────────┬─────────┘
         │
         │ GET /api/parts/catalog?lang=he
         ▼
┌──────────────────┐
│  Backend         │
│                  │
│  catalog = {}    │
│  for part:       │
│    catalog[      │
│      category.   │
│      get_name(   │
│        'he'      │  ← Переводит категорию
│      )           │
│    ].append({    │
│      'id': 15,   │
│      'name':     │
│        part.     │
│        get_name( │
│          'he'    │  ← Переводит запчасть
│        )         │
│    })            │
└────────┬─────────┘
         │
         │ Возвращает:
         │ {
         │   "מנוע": [
         │     {"id": 15, "name": "פילטר שמן"}
         │   ]
         │ }
         ▼
┌──────────────────┐
│  Браузер         │
│                  │
│  Механик видит:  │
│  מנוע            │
│  □ פילטר שמן     │
│                  │
│  Создает заказ   │
└────────┬─────────┘
         │
         │ POST /api/submit_order
         │ {
         │   "category": "Двигатель",
         │   "selected_parts": [
         │     {
         │       "part_id": 15,      ← Отправляет ID!
         │       "name": "פילטר שמן",
         │       "quantity": 2
         │     }
         │   ]
         │ }
         ▼
┌──────────────────┐
│  Backend         │
│                  │
│  Сохраняет в БД: │
│  selected_parts: │
│  [               │
│    {             │
│      part_id: 15,│  ← Сохранен ID
│      name: "...", │
│      quantity: 2 │
│    }             │
│  ]               │
└──────────────────┘
```

### 2️⃣ Админ открывает панель (всегда русский)

```
┌──────────────────┐
│  Браузер         │
│  (Админ-панель)  │
└────────┬─────────┘
         │
         │ GET /api/orders
         ▼
┌──────────────────┐
│  Backend         │
│                  │
│  orders = []     │
│  for order:      │
│    orders.append(│
│      order.      │
│      to_dict(    │
│        lang='ru' │  ← ВСЕГДА РУССКИЙ!
│      )           │
│    )             │
│                  │
│  В to_dict():    │
│  for part in     │
│    selected_     │
│    parts:        │
│                  │
│    part_id =     │
│      part['part_ │
│      id']        │  ← Берет ID
│                  │
│    part_obj =    │
│      Part.query. │
│      get(part_id)│  ← Находит в БД
│                  │
│    name =        │
│      part_obj.   │
│      get_name(   │
│        'ru'      │  ← Переводит на русский
│      )           │
└────────┬─────────┘
         │
         │ Возвращает:
         │ {
         │   "id": 123,
         │   "category": "Двигатель",    ← RU
         │   "selected_parts": [
         │     {
         │       "part_id": 15,
         │       "name": "Масляный фильтр", ← RU
         │       "quantity": 2
         │     }
         │   ]
         │ }
         ▼
┌──────────────────┐
│  Браузер         │
│                  │
│  Админ видит:    │
│  Категория:      │
│  Двигатель       │
│                  │
│  Запчасти:       │
│  • Масляный      │
│    фильтр (x2)   │
└──────────────────┘
```

### 3️⃣ Telegram уведомление (русский)

```
┌──────────────────┐
│  Backend         │
│                  │
│  notify_admin_   │
│  new_order()     │
│                  │
│  for part in     │
│    order.        │
│    selected_     │
│    parts:        │
│                  │
│    part_id =     │
│      part['part_ │
│      id']        │  ← Берет ID
│                  │
│    part_obj =    │
│      Part.query. │
│      get(part_id)│  ← Находит в БД
│                  │
│    name =        │
│      part_obj.   │
│      get_name(   │
│        'ru'      │  ← Переводит на русский
│      )           │
└────────┬─────────┘
         │
         │ Отправляет в Telegram:
         │
         │ 🔔 Новый заказ
         │ 📦 Категория: Двигатель
         │ Детали:
         │ • Масляный фильтр (x2)
         ▼
┌──────────────────┐
│  Telegram        │
│                  │
│  Админ получает  │
│  уведомление     │
│  на русском      │
└──────────────────┘
```

## Ключевые моменты

### ✅ Почему это работает?

1. **Сохранение ID вместо названия**
   - Механик видит: "פילטר שמן"
   - В базу сохраняется: `part_id: 15` + `name: "פילטר שמן"`
   - При отображении: `Part.get(15).get_name('ru')` → "Масляный фильтр"

2. **Единый справочник**
   - Одна запись в таблице `parts`
   - Три поля с названиями: `name_ru`, `name_en`, `name_he`
   - Метод `get_name(lang)` выбирает нужное поле

3. **Автоматический перевод**
   - `order.to_dict(lang='ru')` всегда переводит на русский
   - Админ не видит разницы, на каком языке был создан заказ

### ⚠️ Обработка ошибок

**Если запчасть не найдена по ID:**
```python
if part_id:
    part_obj = Part.query.get(part_id)
    if part_obj:
        name = part_obj.get_name('ru')
    else:
        name = part.get('name', '')  # Fallback на сохраненное название
else:
    name = part.get('name', '')  # Старый формат без ID
```

## Сравнение подходов

### ❌ Старый подход (без перевода)

```
Механик (иврит):        Админ видит:
מנוע                    מנוע          ← Непонятно!
פילטר שמן               פילטר שמן     ← Непонятно!
```

### ✅ Новый подход (с переводом)

```
Механик (иврит):        Админ видит:
מנוע                    Двигатель     ← Понятно!
פילטר שמן               Масляный      ← Понятно!
                        фильтр
```

## Производительность

### Оптимизация запросов:

```python
# ❌ Плохо: N+1 запросов
for part in selected_parts:
    Part.query.get(part['part_id'])  # Запрос для каждой запчасти

# ✅ Хорошо: 1 запрос
part_ids = [p['part_id'] for p in selected_parts if p.get('part_id')]
parts_dict = {p.id: p for p in Part.query.filter(Part.id.in_(part_ids)).all()}

for part in selected_parts:
    part_obj = parts_dict.get(part['part_id'])
```

**Примечание:** Текущая реализация использует простой подход (N+1). Для оптимизации при большом количестве заказов можно добавить кеширование или батч-запросы.

---

**Документация:** См. другие файлы для деталей реализации
