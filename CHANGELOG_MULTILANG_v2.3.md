# Changelog - Многоязычная система заказов

## v2.3 - Умный перевод заказов (5 ноября 2025)

### 🌍 Новые возможности

#### Многоязычные заказы
- ✅ Механик создает заказ на своем языке (русский/английский/иврит)
- ✅ Админ всегда видит заказы на русском языке
- ✅ Telegram уведомления админу приходят на русском
- ✅ Печатные чеки формируются на русском
- ✅ Автоматический перевод категорий и запчастей

### 🔧 Технические изменения

#### Backend (app.py)

**API каталога запчастей:**
```python
GET /api/parts/catalog?lang=he
# Возвращает:
{
  "категория_на_иврите": [
    {"id": 1, "name": "запчасть_на_иврите"},
    ...
  ]
}
```

**API заказов для админа:**
```python
GET /api/orders
# Автоматически возвращает все заказы на русском языке
# order.to_dict(lang='ru')
```

**Уведомления:**
- `notify_admin_new_order()` - переводит на русский
- `print_receipt()` - печатает на русском

#### Models (models.py)

**Order.to_dict():**
- Новый параметр `lang` для указания языка перевода
- Автоматический перевод категорий через `Category.get_name(lang)`
- Автоматический перевод запчастей через `Part.get_name(lang)`
- Поддержка `part_id` для получения названия из базы

#### Frontend

**mechanic.html:**
- Поддержка нового формата каталога с ID
- Сохранение `part_id` при выборе запчасти
- Отправка `part_id` вместе с названием в API

**mechanic/order_form.html:**
- Аналогичная поддержка для авторизованных механиков
- Динамическая загрузка каталога через API
- Работа с объектами `{id, name}` вместо просто строк

### 💾 Формат данных

#### Новый формат selected_parts:
```json
[
  {
    "part_id": 15,           // ID для перевода
    "name": "פילטר שמן",     // Название на языке механика
    "quantity": 2             // Количество
  }
]
```

#### Старый формат (поддерживается):
```json
[
  {"name": "Масляный фильтр", "quantity": 2},
  "Воздушный фильтр"
]
```

### 🔄 Обратная совместимость

- ✅ Поддержка старого формата запчастей (массив строк)
- ✅ Поддержка промежуточного формата (объекты без `part_id`)
- ✅ Поддержка нового формата (объекты с `part_id`)
- ✅ Fallback на сохраненное название, если запчасть не найдена

### 📝 Пример работы

**Шаг 1: Механик (язык: иврит)**
```
Выбирает категорию: "מנוע"
Выбирает запчасти:
- פילטר שמן (x2)
- פילטר אוויר (x1)

Создает заказ → сохраняется:
{
  "category": "Двигатель",
  "selected_parts": [
    {"part_id": 15, "name": "פילטר שמן", "quantity": 2},
    {"part_id": 23, "name": "פילטר אוויר", "quantity": 1}
  ]
}
```

**Шаг 2: Админ открывает админ-панель**
```
GET /api/orders

Получает:
{
  "category": "Двигатель",           // Переведено!
  "selected_parts": [
    {"part_id": 15, "name": "Масляный фильтр", "quantity": 2},
    {"part_id": 23, "name": "Воздушный фильтр", "quantity": 1}
  ]
}
```

**Шаг 3: Telegram уведомление**
```
🔔 Новый заказ от Иван
📋 Заказ №123
📦 Категория: Двигатель

Детали:
• Масляный фильтр (x2)
• Воздушный фильтр (x1)
```

### 🎯 Преимущества

1. **Для механиков:**
   - Работа на родном языке
   - Удобный интерфейс
   - Быстрое создание заказов

2. **Для администратора:**
   - Все заказы на русском
   - Понятные уведомления
   - Удобная печать чеков

3. **Для системы:**
   - Один справочник на 3 языка
   - Нет дублирования данных
   - Легко добавить новые языки

### 📚 Документация

- `MULTILANG_ORDERS_FEATURE.md` - Подробное техническое описание
- `MULTILANG_ORDERS_QUICK_START.md` - Быстрый старт и примеры

### 🐛 Исправления

- Исправлена обработка категорий в разных языках
- Улучшена обратная совместимость со старыми заказами
- Оптимизированы SQL запросы для перевода

### ⚠️ Breaking Changes

Нет! Все изменения обратно совместимы.

### 🚀 Миграция

Не требуется! Система автоматически работает с:
- Старыми заказами (без `part_id`)
- Новыми заказами (с `part_id`)

### 📋 Чеклист для деплоя

- [ ] Убедитесь, что все запчасти имеют переводы (`name_ru`, `name_en`, `name_he`)
- [ ] Протестируйте создание заказа на разных языках
- [ ] Проверьте отображение в админ-панели
- [ ] Проверьте Telegram уведомления
- [ ] Протестируйте печать чека
- [ ] Деплой на Render

---

**Дата релиза:** 5 ноября 2025  
**Версия:** v2.3  
**Автор:** Felix Hub Team
