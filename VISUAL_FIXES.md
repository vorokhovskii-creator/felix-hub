# 🎨 Визуальные исправления главной страницы

## Проблемы, которые были исправлены:

### ❌ Проблема 1: Флаги не отображались
**Было:** `��` вместо флагов

**Причина:** 
- Emoji символы не были обернуты в специальный тег
- Не указан правильный шрифт для emoji

**Решение:**
```html
<!-- Было -->
<button class="lang-flag">🇷🇺</button>

<!-- Стало -->
<button type="button" class="lang-flag">
    <span>🇷🇺</span>
</button>
```

```css
.lang-flag span {
    display: inline-block;
    font-family: "Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji", sans-serif;
}
```

---

### ❌ Проблема 2: Флаги меняли порядок при нажатии

**Было:** 
- При активации флаг мог "прыгать"
- Порядок менялся из-за flexbox

**Причина:**
- `flex-wrap: wrap` позволял переносить элементы
- Не зафиксирован порядок элементов

**Решение:**
```css
.lang-buttons {
    display: flex;
    gap: 10px;
    justify-content: center;
    margin-bottom: 30px;
    flex-wrap: nowrap;  /* ← ФИКСИРУЕТ ПОРЯДОК */
}

.lang-flag {
    /* ... */
    order: 0;  /* ← НЕ ПОЗВОЛЯЕТ МЕНЯТЬ ПОРЯДОК */
}
```

---

## ✅ Итоговый результат:

### Порядок флагов (ВСЕГДА):
```
🇷🇺  🇬🇧  🇮🇱
```

### Состояния кнопок:

**Неактивная:**
- Белый фон
- Серая рамка (#ddd)

**Активная:**
- Голубой фон (#f0f4ff)
- Синяя рамка (#667eea)
- Тень от рамки

**При наведении:**
- Увеличение масштаба (1.1x)
- Синяя рамка

---

## 📱 Адаптация для мобильных:

```css
@media (max-width: 480px) {
    .lang-flag {
        font-size: 1.3em;    /* Меньше для мобильных */
        padding: 6px 12px;   /* Компактнее */
    }
}
```

---

## 🔧 Технические детали:

### HTML структура:
```html
<div class="lang-buttons">
    <button type="button" 
            class="lang-flag {% if g.locale == 'ru' %}active{% endif %}" 
            onclick="switchLanguage('ru')" 
            title="Русский">
        <span>🇷🇺</span>
    </button>
    <!-- ... аналогично для EN и HE -->
</div>
```

### JavaScript функция смены языка:
```javascript
function switchLanguage(lang) {
    fetch('/set_language/' + lang, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        }
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            window.location.reload();  // Перезагрузка с новым языком
        }
    });
}
```

---

## 🎯 Что теперь работает:

✅ Флаги отображаются правильно на всех платформах
✅ Порядок языков ВСЕГДА: Русский → English → עברית
✅ Активный язык подсвечивается синим
✅ Плавная анимация при наведении
✅ Работает на мобильных устройствах
✅ Поддержка RTL для иврита
