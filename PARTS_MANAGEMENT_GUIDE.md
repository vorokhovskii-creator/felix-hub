# Руководство по управлению запчастями

## Обзор

В версии 2.2.1 Felix Hub добавлена функциональность управления справочником запчастей. Теперь администратор может добавлять, редактировать и удалять запчасти через веб-интерфейс.

## Основные возможности

### 📦 Для администратора

1. **Просмотр всех запчастей**
   - Список всех запчастей с группировкой по категориям
   - Фильтрация по статусу (активные/неактивные)
   - Поиск по названию

2. **Добавление запчастей**
   - Добавление одной запчасти через форму
   - Массовое добавление через API
   - Импорт дефолтного каталога

3. **Редактирование запчастей**
   - Изменение названия
   - Изменение категории
   - Изменение порядка сортировки
   - Активация/деактивация

4. **Удаление запчастей**
   - Удаление отдельных запчастей
   - Деактивация вместо удаления (рекомендуется)

### 🔧 Для механиков

- Форма заказа автоматически загружает актуальный каталог запчастей из базы данных
- Отображаются только активные запчасти
- Возможность добавления запчастей вручную

## Доступ к функциям

### Панель администратора

1. Войдите в админ-панель: `/admin/login`
2. Перейдите в раздел "📦 Запчасти"
3. URL: `/admin/parts`

### Интерфейс управления

```
📦 Управление запчастями
├── 📊 Статистика (всего, активных, категорий)
├── 🔍 Фильтры (поиск, статус)
├── 📑 Вкладки категорий
├── 📋 Список запчастей (карточки)
└── ➕ Кнопка добавления
```

## API Endpoints

### Получение каталога

```http
GET /api/parts/catalog?active_only=true
```

Возвращает весь каталог в формате:
```json
{
  "Тормоза": ["Передние колодки", "Задние колодки"],
  "Двигатель": ["Масло моторное", "Масляный фильтр"]
}
```

### Получение списка запчастей

```http
GET /api/parts?category=Тормоза&active_only=true
```

Параметры:
- `category` (optional) - фильтр по категории
- `active_only` (optional) - только активные (true/false)

### Получение категорий

```http
GET /api/parts/categories
```

Возвращает массив категорий:
```json
["Тормоза", "Двигатель", "Подвеска", "Электрика", "Расходники"]
```

### Создание запчасти (только админ)

```http
POST /api/admin/parts
Content-Type: application/json

{
  "name": "Передние колодки",
  "category": "Тормоза",
  "is_active": true,
  "sort_order": 0
}
```

### Обновление запчасти (только админ)

```http
PUT /api/admin/parts/{id}
Content-Type: application/json

{
  "name": "Передние колодки (обновлено)",
  "category": "Тормоза",
  "is_active": true,
  "sort_order": 0
}
```

### Удаление запчасти (только админ)

```http
DELETE /api/admin/parts/{id}
```

### Переключение активности (только админ)

```http
PUT /api/admin/parts/{id}/toggle-active
```

### Импорт дефолтного каталога (только админ)

```http
POST /api/admin/parts/import-default
```

Импортирует стандартный набор запчастей (пропускает существующие).

### Массовое создание (только админ)

```http
POST /api/admin/parts/bulk-create
Content-Type: application/json

{
  "parts": [
    {
      "name": "Деталь 1",
      "category": "Категория",
      "is_active": true,
      "sort_order": 0
    },
    {
      "name": "Деталь 2",
      "category": "Категория",
      "is_active": true,
      "sort_order": 1
    }
  ]
}
```

## Модель данных

### Таблица `parts`

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| name | String(250) | Название запчасти |
| category | String(120) | Категория (с индексом) |
| is_active | Boolean | Активна ли запчасть (с индексом) |
| sort_order | Integer | Порядок сортировки |
| created_at | DateTime | Дата создания |
| updated_at | DateTime | Дата обновления |

## Миграция базы данных

Для добавления таблицы запчастей выполните:

```bash
python migrations/add_parts_table.py
```

Это создаст таблицу `parts` и импортирует дефолтный каталог.

## Примеры использования

### JavaScript: Загрузка каталога

```javascript
async function loadCatalog() {
    const response = await fetch('/api/parts/catalog?active_only=true');
    const catalog = await response.json();
    console.log(catalog);
}
```

### JavaScript: Создание запчасти

```javascript
async function createPart(name, category) {
    const response = await fetch('/api/admin/parts', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            name: name,
            category: category,
            is_active: true,
            sort_order: 0
        })
    });
    
    const result = await response.json();
    return result;
}
```

### Python: Добавление запчасти программно

```python
from models import Part, db

part = Part(
    name='Новая запчасть',
    category='Тормоза',
    is_active=True,
    sort_order=0
)

db.session.add(part)
db.session.commit()
```

## Рекомендации

1. **Не удаляйте запчасти**, используйте деактивацию
   - Это сохранит историю заказов
   - Позволит восстановить запчасть при необходимости

2. **Используйте осмысленные названия**
   - Понятные для всех механиков
   - Без сокращений (если возможно)

3. **Группируйте по категориям**
   - Облегчает поиск
   - Улучшает UX для механиков

4. **Порядок сортировки**
   - Более популярные запчасти - выше
   - Используйте шаг 10 (0, 10, 20...) для возможности вставки

5. **Регулярно проверяйте каталог**
   - Удаляйте устаревшие позиции
   - Добавляйте новые по запросам

## Troubleshooting

### Запчасти не загружаются в форме заказа

1. Проверьте, что таблица `parts` создана:
   ```bash
   python migrations/add_parts_table.py
   ```

2. Проверьте, что есть активные запчасти:
   ```bash
   curl http://localhost:8000/api/parts/catalog?active_only=true
   ```

### Ошибка при создании запчасти

- Проверьте уникальность (название + категория)
- Убедитесь, что вы авторизованы как админ
- Проверьте формат данных (JSON)

### Дубликаты при импорте

- Нормально, импорт пропускает существующие записи
- Проверьте вывод миграции для деталей

## Будущие улучшения

- [ ] Добавление артикулов запчастей
- [ ] Связь с поставщиками
- [ ] История изменения цен
- [ ] Экспорт/импорт каталога в Excel
- [ ] Фотографии запчастей
- [ ] Автоматические подсказки при вводе

## Changelog

### v2.2.1 (03.11.2024)
- ✅ Добавлена модель `Part`
- ✅ API для управления запчастями
- ✅ Веб-интерфейс для администратора
- ✅ Интеграция с формой заказа
- ✅ Миграция базы данных
- ✅ Импорт дефолтного каталога

---

**Автор:** Felix Hub Development Team  
**Дата:** 03.11.2024  
**Версия документа:** 1.0
