# 📱 Исправление прокрутки select на мобильных устройствах

## Проблема
В мобильной версии приложения модальное окно выбора категории запчастей не имело прокрутки, что делало невозможным выбор категорий, находящихся за пределами видимой области экрана.

## Решение

### 1. Обновление файла `templates/mechanic.html`

#### Добавлены стили для select элемента:
```css
.form-group select {
    -webkit-appearance: menulist;
    appearance: menulist;
    background-color: white;
    cursor: pointer;
}
```

#### Добавлены мобильные стили:
```css
@media (max-width: 480px) {
    /* Фиксируем прокрутку для select на мобильных устройствах */
    .form-group select {
        font-size: 16px; /* Предотвращает зум на iOS */
        max-height: none; /* Убираем ограничение высоты */
        -webkit-appearance: menulist-button; /* Нативный вид для iOS */
        appearance: menulist-button;
    }
    
    /* Для Android */
    .form-group select option {
        font-size: 16px;
    }
}
```

### 2. Обновление файла `static/css/mobile-responsive.css`

Добавлены стили в секцию FORMS для улучшенной работы с select на мобильных:

```css
@media (max-width: 768px) {
    /* Исправление прокрутки для select на мобильных */
    .form-group select {
        -webkit-appearance: menulist-button;
        appearance: menulist-button;
        max-height: none;
        overflow-y: auto;
        -webkit-overflow-scrolling: touch;
    }
    
    .form-group select option {
        font-size: 16px;
        padding: 10px;
    }
}
```

### 3. Обновление файла `templates/mechanic/order_form.html`

#### Добавлены базовые стили для select:
```css
.form-group select {
    -webkit-appearance: menulist;
    appearance: menulist;
    background-color: white;
    cursor: pointer;
}
```

#### Добавлены мобильные стили:
```css
@media (max-width: 768px) {
    /* Исправление прокрутки для select на мобильных */
    select {
        font-size: 16px !important;
        -webkit-appearance: menulist-button;
        appearance: menulist-button;
        max-height: none;
    }
    
    select option {
        font-size: 16px;
    }
}
```

## Технические детали

### Ключевые изменения:

1. **`-webkit-appearance: menulist-button`** - использует нативный системный стиль для выпадающих списков на iOS
2. **`appearance: menulist-button`** - стандартное свойство для кроссбраузерной поддержки
3. **`font-size: 16px`** - предотвращает автоматическое масштабирование на iOS при фокусе
4. **`max-height: none`** - убирает ограничения по высоте, позволяя системе управлять высотой
5. **`-webkit-overflow-scrolling: touch`** - включает плавную прокрутку на iOS

### Поддерживаемые платформы:
- ✅ iOS Safari
- ✅ Android Chrome
- ✅ Мобильные браузеры на базе WebKit
- ✅ Desktop браузеры (совместимость сохранена)

## Тестирование

### Тестировать на:
1. **iPhone (Safari)**
   - Откройте форму заказа
   - Нажмите на поле "Категория запчастей"
   - Убедитесь, что список прокручивается

2. **Android (Chrome)**
   - Откройте форму заказа
   - Нажмите на поле "Категория запчастей"
   - Убедитесь, что список прокручивается

3. **Desktop браузеры**
   - Убедитесь, что select работает как обычно

## Результат
✅ Модальное окно выбора категории теперь корректно прокручивается на всех мобильных устройствах
✅ Сохранена совместимость с desktop версией
✅ Улучшен UX для мобильных пользователей

## Дата исправления
5 ноября 2025 г.
