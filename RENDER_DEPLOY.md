# 🚀 Деплой Felix Hub на Render

## 📋 Инструкция по деплою

### 1. Подготовка репозитория

Убедитесь, что все изменения закоммичены:

```bash
git add .
git commit -m "Fix: PostgreSQL URL and Gunicorn config for Render"
git push origin main
```

### 2. Настройка на Render

1. Зайдите на https://render.com
2. Создайте новый **Web Service**
3. Подключите ваш GitHub репозиторий
4. Render автоматически найдет `render.yaml`

### 3. Проверьте переменные окружения

В Render Dashboard → Environment:

```
SECRET_KEY = (автоматически сгенерирован)
DATABASE_URL = (автоматически из PostgreSQL)
TELEGRAM_BOT_TOKEN = ваш_токен_бота
TELEGRAM_ADMIN_CHAT_ID = ваш_chat_id
ALLOW_ANONYMOUS_ORDERS = true
FLASK_ENV = production
```

### 4. Инициализация базы данных

После первого деплоя выполните миграцию:

```bash
# В Render Shell (или локально с правильным DATABASE_URL)
python migrations/migrate_v2_2.py
```

Или создайте механика вручную через админ-панель после запуска.

### 5. Первый вход

После успешного деплоя:

**Админ:**
- URL: `https://felix-hub.onrender.com/admin/login`
- Пароль: `felix2025`

**Механик (после создания через админку):**
- URL: `https://felix-hub.onrender.com/mechanic/login`
- Используйте созданные credentials

### 6. Создание первого механика

1. Войдите в админ-панель
2. Перейдите на вкладку "Механики"
3. Нажмите "Добавить механика"
4. Заполните данные:
   - Username: `mihail`
   - Полное имя: `Mihail Vorokhovsky`
   - Пароль: `felix2025`
   - Отметьте "Активен"
5. Сохраните

## 🔧 Изменения для Render

### app.py
- ✅ Исправлен URL PostgreSQL (postgres:// → postgresql://)
- ✅ Поддержка переменной окружения PORT

### render.yaml
- ✅ Команда запуска: `gunicorn --bind 0.0.0.0:$PORT app:app`
- ✅ Добавлена переменная ALLOW_ANONYMOUS_ORDERS
- ✅ PostgreSQL база данных настроена

### requirements.txt
- ✅ gunicorn==21.2.0
- ✅ psycopg2-binary==2.9.9
- ✅ Все зависимости включены

## 🐛 Решение проблем

### Ошибка 404 на /admin/login

**Причина:** Приложение не запускается или неправильная конфигурация Gunicorn

**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь, что `startCommand: gunicorn --bind 0.0.0.0:$PORT app:app`
3. Проверьте, что DATABASE_URL правильно настроен

### База данных не инициализируется

**Причина:** Таблицы не созданы

**Решение:**
```bash
# В Render Shell
python -c "from app import app, db; app.app_context().push(); db.create_all()"
```

### Telegram не работает

**Причина:** Переменные окружения не установлены

**Решение:**
1. В Render Dashboard → Environment
2. Добавьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_ADMIN_CHAT_ID`

### Ошибка подключения к PostgreSQL

**Причина:** Неправильный формат URL

**Решение:**
- Код в app.py автоматически исправляет `postgres://` на `postgresql://`
- Если проблема сохраняется, проверьте логи

## 📊 Мониторинг

### Проверка здоровья сервиса

Render автоматически проверяет `/` каждые 30 секунд.

### Логи

Просмотр логов в реальном времени:
```
Render Dashboard → Logs
```

### Метрики

Render показывает:
- CPU usage
- Memory usage
- Request count
- Response time

## 🔒 Безопасность

### Обязательно измените:

1. **Admin пароль** в `app.py`:
   ```python
   # Строка 363
   if request.form.get('password') == 'felix2025':
   ```
   Замените на более безопасный или используйте переменную окружения.

2. **SECRET_KEY**: Используйте автогенерированный Render или создайте свой

3. **PostgreSQL credentials**: Автоматически управляются Render

## 🚀 Оптимизация для продакшн

### 1. Кеширование (опционально)
Добавьте Redis для сессий:
```yaml
# В render.yaml
  - type: redis
    name: felix-hub-redis
    plan: free
```

### 2. CDN для статики
Используйте Cloudflare или другой CDN для папки `/static`

### 3. Мониторинг ошибок
Добавьте Sentry:
```bash
pip install sentry-sdk[flask]
```

### 4. Резервное копирование БД
Render автоматически создает бэкапы на платных планах.

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте переменные окружения
3. Убедитесь, что все файлы закоммичены в Git
4. Проверьте, что PostgreSQL база создана и подключена

---

**Статус деплоя:** ✅ Готово к production  
**Последнее обновление:** 3 ноября 2025
