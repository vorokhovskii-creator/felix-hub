# Быстрый старт - Многоязычные заказы

## Что изменилось?

Теперь механик может работать на **любом языке** (русский, английский, иврит), а админ всегда будет видеть заказы **на русском языке**.

## Как это работает?

### Для механика:

1. **Выбирает язык** при входе или в настройках профиля
2. **Видит интерфейс** на выбранном языке
3. **Создает заказ** - категории и запчасти отображаются на его языке
4. **Заказ сохраняется** с ID запчастей в базе данных

### Для администратора:

1. **Открывает админ-панель** - всегда на русском
2. **Видит все заказы** с переведенными на русский категориями и запчастями
3. **Получает Telegram уведомления** на русском
4. **Печатает чеки** на русском

## Пример

**Механик (иврит):**
```
Категория: מנוע (Двигатель)
Запчасти:
- פילטר שמן x2 (Масляный фильтр)
- פילטר אוויר x1 (Воздушный фильтр)
```

**Админ видит:**
```
Категория: Двигатель
Запчасти:
- Масляный фильтр x2
- Воздушный фильтр x1
```

## Технические детали

### Формат данных в базе:

```json
{
  "selected_parts": [
    {
      "part_id": 15,
      "name": "פילטר שמן",
      "quantity": 2
    }
  ]
}
```

- `part_id` - ID запчасти для перевода
- `name` - название на языке механика (резервное значение)
- `quantity` - количество

### API:

**Получение каталога (механик):**
```
GET /api/parts/catalog?lang=he
```

Ответ:
```json
{
  "מנוע": [
    {"id": 15, "name": "פילטר שמן"},
    {"id": 23, "name": "פילטר אוויר"}
  ]
}
```

**Получение заказов (админ):**
```
GET /api/orders
```

Ответ автоматически на русском:
```json
[
  {
    "id": 1,
    "category": "Двигатель",
    "selected_parts": [
      {"part_id": 15, "name": "Масляный фильтр", "quantity": 2}
    ]
  }
]
```

## Что нужно сделать?

### 1. Убедитесь, что в базе есть переводы

Проверьте, что все запчасти имеют названия на трех языках:
- `name_ru` - русский
- `name_en` - английский
- `name_he` - иврит

### 2. Создайте тестового механика

```bash
# Войдите в админ-панель
# Создайте механика с языком "he" (иврит)
```

### 3. Протестируйте

1. Войдите под механиком
2. Создайте заказ на иврите
3. Войдите в админ-панель
4. Убедитесь, что заказ отображается на русском

## Обратная совместимость

✅ Старые заказы (без `part_id`) продолжат работать  
✅ Механики без авторизации (если разрешено) могут создавать заказы  
✅ Постепенный переход - новые заказы используют новый формат  

## Часто задаваемые вопросы

**Q: Что если запчасть не найдена по ID?**  
A: Система использует сохраненное название (`name`) как резервный вариант.

**Q: Можно ли добавить новый язык?**  
A: Да! Добавьте поля `name_xx` в модель `Part` и `Category`, обновите конфигурацию Babel.

**Q: Нужно ли мигрировать старые заказы?**  
A: Нет, система работает со старым форматом. Но для полноценного перевода рекомендуется обновить данные.

## Следующие шаги

1. ✅ Реализована многоязычная система
2. 🔄 Протестируйте на реальных данных
3. 📊 Заполните базу переводами запчастей
4. 🚀 Разверните на production (Render)

---

**Документация:** См. `MULTILANG_ORDERS_FEATURE.md` для технических деталей
