# 📋 Список миграций для многоязычности

## Выполненные миграции

### 1. `migrations/add_multilang_parts.py`
**Назначение:** Добавление переводов для запчастей на английский и иврит

**Что делает:**
- Обновляет переводы для существующих 25 запчастей
- Создает 20 недостающих запчастей с переводами
- Заполняет поля: name_en, name_he для всех запчастей

**Результат:**
- ✅ 47 запчастей с полными переводами на 3 языка

**Запуск:**
```bash
python3 migrations/add_multilang_parts.py
```

---

### 2. `migrations/add_category_translations.py`
**Назначение:** Добавление переводов для категорий на английский и иврит

**Что делает:**
- Добавляет колонки name_en, name_he, name_ru в таблицу categories
- Заполняет переводы для всех 7 категорий

**Результат:**
- ✅ 7 категорий с полными переводами на 3 языка

**Запуск:**
```bash
python3 migrations/add_category_translations.py
```

---

## Последовательность выполнения

Если база данных уже создана, выполните миграции в следующем порядке:

```bash
# Шаг 1: Переводы запчастей
python3 migrations/add_multilang_parts.py

# Шаг 2: Переводы категорий
python3 migrations/add_category_translations.py
```

## Проверка результата

После выполнения миграций проверьте результат:

```bash
python3 -c "
from app import app, db
from models import Part, Category

with app.app_context():
    total_cats = Category.query.count()
    cats_en = Category.query.filter(Category.name_en.isnot(None)).count()
    cats_he = Category.query.filter(Category.name_he.isnot(None)).count()
    
    total_parts = Part.query.count()
    parts_en = Part.query.filter(Part.name_en.isnot(None)).count()
    parts_he = Part.query.filter(Part.name_he.isnot(None)).count()
    
    print(f'✅ Категории: {cats_en}/{total_cats} EN, {cats_he}/{total_cats} HE')
    print(f'✅ Запчасти: {parts_en}/{total_parts} EN, {parts_he}/{total_parts} HE')
"
```

Ожидаемый результат:
```
✅ Категории: 7/7 EN, 7/7 HE
✅ Запчасти: 47/47 EN, 47/47 HE
```

## Откат миграций

В случае необходимости отката миграций:

⚠️ **ВНИМАНИЕ:** SQLite не поддерживает DROP COLUMN, поэтому откат возможен только через:
1. Создание резервной копии базы данных
2. Или удаление и пересоздание таблиц

Рекомендуется создать резервную копию перед миграцией:

```bash
# Создать резервную копию
cp instance/felix_hub.db instance/felix_hub_backup_$(date +%Y%m%d_%H%M%S).db
```

## Дополнительная информация

- Полный отчет: `MULTILANG_PARTS_REPORT.md`
- Краткое руководство: `MULTILANG_PARTS_QUICKSTART.md`
- Модели с поддержкой многоязычности: `models.py`

---

**Дата создания:** 04.11.2024  
**Версия:** Felix Hub v2.2  
**Статус:** ✅ Готово к использованию
