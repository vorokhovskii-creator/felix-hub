# 📚 Инструкция по массовому импорту запчастей

## 🎯 Назначение

Этот документ описывает процесс массового добавления запчастей в базу данных Felix Hub.

---

## 📋 Файлы для работы

1. **`add_parts_bulk.py`** - Основной скрипт для импорта запчастей
2. **`verify_parts.py`** - Скрипт проверки добавленных запчастей

---

## 🚀 Быстрый старт

### Шаг 1: Подготовка данных

Отредактируйте файл `add_parts_bulk.py`, найдите массив `PARTS_DATA` и добавьте ваши запчасти:

```python
PARTS_DATA = [
    {
        "name_ru": "Название на русском",
        "name_en": "English name",
        "name_he": "שם בעברית",
        "category": "Категория"
    },
    # ... добавьте больше запчастей
]
```

### Шаг 2: Запуск импорта

```bash
python add_parts_bulk.py
```

### Шаг 3: Проверка результатов

```bash
python verify_parts.py
```

---

## 📝 Формат данных

### Обязательные поля:
- `name_ru` - Название на русском (обязательно)
- `name_en` - Название на английском
- `name_he` - Название на иврите
- `category` - Категория запчасти

### Автоматически устанавливаемые поля:
- `name` - Копируется из `name_ru` (для обратной совместимости)
- `is_active` - Устанавливается в `True`
- `sort_order` - Устанавливается в `0`

---

## 🗂️ Категории

Скрипт автоматически создаёт категории, если они не существуют.

**Существующие категории:**
- Лампочки
- типуль
- жидкости\масла
- тормоза
- типуль\кузов
- Двигатель
- Подвеска
- Расходники
- Электрика
- Типуль
- Тормоза
- Добавки

**Примечание:** Обратите внимание на регистр букв в названиях категорий!

---

## ✨ Особенности скрипта

### 1. Проверка дубликатов
Скрипт автоматически проверяет существование запчастей:
```
- Проверяется комбинация: name_ru + category
- Существующие запчасти пропускаются
```

### 2. Автоматическое создание категорий
```
- Категории создаются при первом использовании
- Название категории сохраняется в поле name_ru
```

### 3. Обработка ошибок
```
- При ошибке запчасть пропускается
- Транзакция откатывается
- Процесс продолжается со следующей запчасти
```

### 4. Подробное логирование
```
- Прогресс [N/Total]
- Статус каждой запчасти (✅/⏭️/❌)
- ID добавленных запчастей
- Итоговая статистика
```

---

## 📊 Пример вывода

```
======================================================================
🚀 Массовое добавление запчастей в Felix Hub
======================================================================

📂 Проверка категорий...
   Необходимо категорий: 5
   ✅ Категория 'Лампочки' существует
   ⚠️  Категория 'тормоза' не найдена, создаём...
   ✅ Все категории готовы

📦 Начинаем добавление 38 запчастей...
   [1/38] ✅ 'Лампа, один контакт' - добавлена (ID: 54)
   [2/38] ⏭️  'Лампа H7' - уже существует
   ...

======================================================================
📊 ИТОГИ:
   ✅ Добавлено:   35
   ⏭️  Пропущено:  3
   ❌ Ошибки:      0
   📦 Всего:       38
======================================================================
```

---

## 🔧 Расширенное использование

### Добавление описаний

Можно добавить описания запчастей:

```python
{
    "name_ru": "Фильтр масляный",
    "name_en": "Oil filter",
    "name_he": "מסנן שמן",
    "category": "типуль",
    "description_ru": "Фильтр для очистки моторного масла",
    "description_en": "Filter for engine oil cleaning",
    "description_he": "מסנן לניקוי שמן מנוע"
}
```

### Изменение порядка сортировки

```python
{
    "name_ru": "Важная запчасть",
    "name_en": "Important part",
    "name_he": "חלק חשוב",
    "category": "типуль",
    "sort_order": 1  # Будет выше в списке
}
```

---

## ⚠️ Важные замечания

1. **Кодировка:** Файл должен быть в UTF-8 для корректного отображения кириллицы и иврита
2. **Уникальность:** Комбинация `name_ru + category` должна быть уникальной
3. **Категории:** Названия категорий чувствительны к регистру
4. **Резервное копирование:** Рекомендуется создать бэкап базы перед импортом

---

## 🐛 Устранение неполадок

### Ошибка: "Category not found"
**Решение:** Категория будет создана автоматически

### Ошибка: "Already exists"
**Решение:** Запчасть с таким названием уже есть в этой категории, она будет пропущена

### Ошибка: "Database locked"
**Решение:** Закройте другие подключения к базе данных

### Ошибка кодировки
**Решение:** Убедитесь, что файл сохранён в UTF-8:
```python
# В начале файла должно быть:
# -*- coding: utf-8 -*-
```

---

## 📈 Проверка результатов

### Через веб-интерфейс:
1. Войдите в админ-панель: `http://localhost:8000/admin/login`
2. Перейдите в раздел "Запчасти"
3. Используйте фильтр по категориям

### Через скрипт:
```bash
python verify_parts.py
```

### Через SQL:
```sql
SELECT category, COUNT(*) as count 
FROM parts 
WHERE is_active = 1 
GROUP BY category;
```

---

## 📞 Дополнительная информация

- Все запчасти добавляются как **активные** (`is_active = True`)
- Поддерживается **3 языка**: Русский, English, עברית
- Автоматически заполняется поле `name` для обратной совместимости
- Записываются временные метки `created_at` и `updated_at`

---

**Последнее обновление:** 5 ноября 2025 г.  
**Версия:** 1.0
