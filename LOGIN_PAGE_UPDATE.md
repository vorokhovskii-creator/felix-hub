# Обновление главной страницы - Форма входа

## Дата: 6 ноября 2025 г.

## Выполненные изменения

### 1. Новая главная страница (`templates/index.html`)

**Было:**
- Выбор языка с перенаправлением на `/mechanic?lang=XX`
- Три больших кнопки для выбора языка
- Только кнопка "Панель администратора"

**Стало:**
- Форма входа в систему (username + password)
- Компактный переключатель языков (флаги)
- Кнопка "Войти"
- Кнопка "Панель администратора"

### 2. Многоязычность

Добавлены переводы на **3 языка**:

#### Русский (ru)
- "Система управления заказами СТО"
- "Имя пользователя"
- "Введите имя пользователя"
- "Пароль"
- "Введите пароль"
- "Войти"
- "Панель администратора"

#### English (en)
- "Auto Service Order Management System"
- "Username"
- "Enter username"
- "Password"
- "Enter password"
- "Login"
- "Admin Panel"

#### עברית (he)
- "מערכת ניהול הזמנות מוסך"
- "שם משתמש"
- "הזן שם משתמש"
- "סיסמה"
- "הזן סיסמה"
- "התחבר"
- "פאנל מנהל"

### 3. Переключатель языков

**Особенности:**
- Компактные кнопки-флаги: 🇷🇺 🇬🇧 🇮🇱
- Фиксированный порядок (не меняется при нажатии)
- Активный язык подсвечивается
- AJAX смена языка без перезагрузки формы
- Поддержка RTL для иврита

### 4. Исправления CSS

- Добавлен `flex-wrap: nowrap` - флаги всегда в одну линию
- Добавлен `order: 0` - порядок не меняется
- Увеличен размер флагов до `1.5em`
- Добавлен специальный шрифт для emoji: `"Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji"`
- Флаги обернуты в `<span>` для правильного отображения
- Добавлено `type="button"` для предотвращения отправки формы

### 5. Flash-сообщения

Добавлены переводы для системных сообщений:
- "Ваш аккаунт деактивирован. Обратитесь к администратору"
- "Добро пожаловать, %(name)s!"
- "Неверное имя пользователя или пароль"

### 6. Обновление маршрута `/mechanic/login`

- POST запрос обрабатывает форму входа
- GET запрос перенаправляет на главную страницу
- Использование `flash()` с переводами через `_l()`
- Сохранение языка в сессии

## Технические детали

### Файлы изменены:
1. `templates/index.html` - новый дизайн с формой входа
2. `app.py` - обновлен маршрут `/mechanic/login`
3. `translations/ru/LC_MESSAGES/messages.po` - русские переводы
4. `translations/en/LC_MESSAGES/messages.po` - английские переводы
5. `translations/he/LC_MESSAGES/messages.po` - переводы на иврит
6. `translations/*/LC_MESSAGES/messages.mo` - скомпилированные переводы

### Новые возможности:
- ✅ Форма входа на главной странице
- ✅ Переключение языка без перезагрузки
- ✅ Правильное отображение флагов emoji
- ✅ Фиксированный порядок кнопок языков
- ✅ Поддержка RTL для иврита
- ✅ Flash-сообщения на выбранном языке
- ✅ Адаптивный дизайн для мобильных устройств

## Тестирование

Для проверки:
1. Откройте главную страницу `/`
2. Переключите язык - порядок флагов должен оставаться: 🇷🇺 🇬🇧 🇮🇱
3. Введите имя пользователя и пароль
4. Нажмите "Войти"
5. Проверьте, что сообщения отображаются на выбранном языке

## Примечания

- Старый файл сохранен как `templates/index_old.html`
- Все переводы скомпилированы командой `pybabel compile -d translations`
- Форма отправляется POST запросом на `/mechanic/login`
- При успешном входе - перенаправление на `/mechanic/dashboard`
- При ошибке - показ flash-сообщения на текущем языке
