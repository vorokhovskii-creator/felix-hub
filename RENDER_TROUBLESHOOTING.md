# 🔧 Решение проблем на Render

**Дата:** 3 ноября 2025  
**Актуальные проблемы:** 
1. ~~Приложение запускается, но затем получает сигнал `term` и останавливается~~ ✅ ИСПРАВЛЕНО
2. **Таблица orders не существует** ⏳ В ПРОЦЕССЕ

---

## ⚠️ ТЕКУЩАЯ ПРОБЛЕМА: Таблица orders не существует

### Ошибка:
```
psycopg2.errors.UndefinedTable) relation "orders" does not exist
```

### Причина:
База данных PostgreSQL не была инициализирована при первом деплое.

### ✅ Решение (автоматическое):

Исправлено в `render.yaml`:
```yaml
buildCommand: pip install -r requirements.txt && python init_render_db.py
```

**Что происходит:**
1. ✅ Изменения отправлены в GitHub
2. ⏳ Render автоматически запустит деплой
3. ⏳ При деплое выполнится `init_render_db.py`
4. ⏳ Таблицы будут созданы

**Отслеживание:**
- Dashboard: https://dashboard.render.com
- Сервис: felix-hub → Events
- Ожидаемое в логах:
  ```
  🔄 Инициализация базы данных...
  ✅ Таблицы созданы
  ```

**Подробная инструкция:** См. `DATABASE_INIT_FIX.md`

---

## 📋 Анализ логов

### Что показывают логи:

```
10.216.26.120 - - [03/Nov/2025:16:57:43 +0000] "GET / HTTP/1.1" 200 4296 "-" "Render/1.0"
[2025-11-03 16:57:47 +0000] [55] [INFO] Handling signal: term
[2025-11-03 16:57:47 +0000] [57] [INFO] Worker exiting (pid: 57)
[2025-11-03 16:57:48 +0000] [55] [INFO] Shutting down: Master
```

**Признаки:**
- ✅ Health checks проходят (код 200)
- ❌ Сервер получает сигнал остановки `term`
- ❌ Gunicorn worker завершается

---

## 🔍 Причины проблемы

### 1. **Недостаточно памяти на Free tier**
Render Free tier имеет лимит **512 MB RAM**. Gunicorn с несколькими workers может превысить этот лимит.

### 2. **Неправильная конфигурация Gunicorn**
По умолчанию Gunicorn запускает много workers, что потребляет много памяти.

### 3. **Таймаут запросов**
Render может останавливать приложение, если запросы обрабатываются слишком долго.

---

## ✅ Решение

### Оптимизирована команда запуска Gunicorn:

**Было:**
```yaml
startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
```

**Стало:**
```yaml
startCommand: gunicorn --workers 1 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT app:app
```

**Объяснение параметров:**
- `--workers 1` - один worker процесс (экономия памяти)
- `--threads 2` - два потока в worker (параллелизм)
- `--timeout 120` - таймаут 120 секунд для долгих запросов
- `--bind 0.0.0.0:$PORT` - слушать на всех интерфейсах

### Исправлена инициализация БД:

**Было:**
```python
if __name__ == '__main__':
    init_db()  # ❌ Не вызывается при запуске через Gunicorn
    app.run(debug=True, host='0.0.0.0', port=8000)
```

**Стало:**
```python
# Инициализация при запуске через Gunicorn
init_db()  # ✅ Всегда вызывается

if __name__ == '__main__':
    app.run(debug=True, host='0.0.0.0', port=8000)
```

---

## 📊 Измененные файлы

### `render.yaml`
```yaml
startCommand: gunicorn --workers 1 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT app:app
```

### `app.py`
```python
# Инициализация при запуске через Gunicorn
init_db()
```

### `init_render_db.py` (новый)
Скрипт для ручной инициализации базы данных на Render.

---

## 🚀 Что делать сейчас

### 1. Дождаться деплоя Render

Render автоматически обнаружит изменения в GitHub и запустит новый деплой.

**Мониторинг:**
1. Зайдите в Render Dashboard
2. Откройте сервис **felix-hub**
3. Перейдите на вкладку **"Events"**
4. Дождитесь:
   - `Build started`
   - `Build successful`
   - `Deploy live`

### 2. Проверить логи после деплоя

В Render Dashboard → Logs должны появиться:

```
✅ База данных инициализирована
[INFO] Starting gunicorn
[INFO] Listening at: http://0.0.0.0:XXXXX
[INFO] Using worker: sync
[INFO] Booting worker with pid: XX
```

**Хорошие признаки:**
- ✅ Нет сигнала `term`
- ✅ Worker продолжает работать
- ✅ Health checks проходят

### 3. Открыть приложение

```
https://felix-hub.onrender.com
```

**Проверьте:**
- ✅ Главная страница открывается
- ✅ `/admin/login` доступен
- ✅ `/mechanic/login` доступен

---

## 🐛 Если проблема повторяется

### Вариант А: Увеличить лимиты Gunicorn

Добавьте в `render.yaml`:
```yaml
startCommand: gunicorn --workers 1 --threads 1 --timeout 300 --max-requests 100 --bind 0.0.0.0:$PORT app:app
```

Новые параметры:
- `--max-requests 100` - перезапуск worker после 100 запросов (освобождение памяти)
- `--timeout 300` - увеличенный таймаут

### Вариант Б: Проверить использование памяти

В Render Dashboard → Metrics:
- Смотрите график **Memory Usage**
- Если близко к 512 MB - приложение убивается OOM killer

### Вариант В: Добавить логирование

В `app.py`:
```python
import logging
logging.basicConfig(level=logging.INFO)
```

### Вариант Г: Инициализировать БД вручную

Через Render Shell:
```bash
python init_render_db.py
```

---

## 📝 Диагностические команды

### Проверить процессы (в Render Shell):
```bash
ps aux
```

### Проверить память:
```bash
free -m
```

### Проверить соединение с БД:
```bash
python -c "from app import db, app; app.app_context().push(); print(db.engine.execute('SELECT 1').scalar())"
```

---

## ✅ Коммиты

```bash
✅ 04176a8: Fix: Add database initialization to Render build command (текущий)
✅ cd79767: Fix: Optimize Gunicorn config for Render Free tier and add DB init
✅ 61ec162: Fix: API decorators to return JSON errors instead of HTML redirects  
✅ 7e9db43: Fix: PostgreSQL URL handling and Gunicorn config for Render deployment
```

---

## 📞 Дополнительные ресурсы

**Render документация:**
- [Deploy a Flask App](https://render.com/docs/deploy-flask)
- [Troubleshooting](https://render.com/docs/troubleshooting-deploys)

**Gunicorn документация:**
- [Settings](https://docs.gunicorn.org/en/stable/settings.html)

---

**Статус:** ✅ Исправления отправлены, ожидание деплоя  
**Следующий шаг:** Мониторинг логов Render
